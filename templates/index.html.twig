{% extends 'base.html.twig' %} {% block title %} Sciety {% endblock %} {% block body %}
<h1>bem vindo</h1>

<a href="{{ path('app_register') }}" class="btn btn-primary">Register</a>
<a href="{{ path('app_login') }}" class="btn btn-danger">Login</a> {% endblock %}